name: master

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
jobs:

  tests:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Builds and Starts
        run: |
          docker-compose up -d
          docker-compose exec drash deno install --unstable --allow-net=realworld_postgres:5432,deno.land --allow-read=. --allow-write=nessie.config.ts,db -f  https://deno.land/x/nessie/cli.ts
          docker-compose exec -T drash bash -c "/root/.deno/bin/nessie migrate"

      - name: Run Integration Tests
        run: |
          docker exec realworld_drash deno test -A --unstable tests/integration/test.ts

      - name: Run Unit Tests
        run: |
          docker exec realworld_drash deno test -A --unstable tests/unit/models/base_model_test.ts

  linter:
      # Only one OS is required since fmt is cross platform
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install Deno
        uses: denolib/setup-deno@master

      - name: Formatter
        run: deno fmt --check --unstable --ignore='src/public,src/vue,src/package.json,src/webpack.config.js'

      - name: Linter
        run: deno lint --unstable --ignore='src/public,src/vue,src/package.json,src/webpack.config.js'
